package basics.pageObjects;

import org.openqa.selenium.chrome.ChromeDriver;

public class HistoryPage extends PageObject {

	protected HistoryPage(ChromeDriver driver, String baseUrl) {
		super(driver, baseUrl);
	}
}
